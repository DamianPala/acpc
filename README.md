# acpc

Thin Python CLI client for the Agent Client Protocol (ACP).

## Install

```bash
pip install acpc
# or
uv tool install acpc
```

## Quick start

```bash
acpc prompt codex "fix the tests"
acpc prompt claude "analyze repo" --model sonnet
```

See `acpc --help` for full usage.
